# Local Goal State

The `/goal` command stores runtime state in `goals.json`.

Runtime files are intentionally ignored by git because they are local working state, not reusable command configuration.
